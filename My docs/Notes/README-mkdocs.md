# Working with mkdocs

When working on the site itself, as opposed to working on published notes, run the server locally and only commit and push when everything is working as desired

## Environment
WSL: Fedora-42

## Running the server locally
```
. ~/mkdocs-venv/bin/activate
cd blog/habitablelife
mkdocs serve
```